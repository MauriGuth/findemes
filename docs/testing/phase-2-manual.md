# Phase 2 — manual test checklist (real phone, API on Railway)

**Before starting**

- Railway → API → Variables: `RAW_EVENT_KEY` (`openssl rand -base64 32`, never change it without bumping `RAW_EVENT_KEY_VERSION`) and `ANTHROPIC_API_KEY`. Without `RAW_EVENT_KEY` the ingest endpoints answer 503 and the app says capture is not available.
- The deploy shows the **Pre-deploy** step with migration `20260923180000_phase2_ingest`.
- The native module changed: rebuild the dev client once (`cd apps/mobile && npx eas-cli@latest build --profile development --platform android`) and install the new APK over the old one.
- The whitelist comes from `Source.packageName`. Until the verified package names are in `catalog.data.ts` and deployed, Ajustes shows "Todavía no hay apps habilitadas para leer." and nothing is captured (steps 3–12 need them).
- After a deploy that changes the whitelist, open Findemes once: the app pushes the new list to the native module on open. With the app closed the module only refreshes it every 6 h.

| #   | Step                                                                                                                                                                                            | Expected                                                                                                                                                                                                                           | OK  |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| 1   | Ajustes → Captura automática → Activar.                                                                                                                                                         | The disclosure opens **before** any Android permission: what we read (the list of apps), what we don't (WhatsApp, messages, any other app), that unrecognized texts go to Claude (Anthropic), 30 days, how to turn it off.         |     |
| 1b  | "Ahora no".                                                                                                                                                                                     | Back in Ajustes, capture still off, no ingest token issued.                                                                                                                                                                        |     |
| 2   | Activar → "Acepto y activo" → "Abrir el permiso" → turn Findemes on.                                                                                                                            | If Android shows "Configuración restringida": "Abrir la info de la app" → ⋮ → "Permitir configuración restringida", then retry. Back in the app the card says the capture is on.                                                   |     |
| 3   | Swipe Findemes away from recents (do **not** use "Forzar detención": Android unbinds the listener of a force-stopped app until it is opened again). Make a real payment with a whitelisted app. | Within 1 minute, without opening Findemes, the movement exists (open the app to check): "· Detectado" on Inicio, the header dropped by the amount.                                                                                 |     |
| 4   | Same payment's notification shown twice (some apps update it).                                                                                                                                  | One movement, not two.                                                                                                                                                                                                             |     |
| 5   | A notification of a whitelisted app that is not money (promo, login alert).                                                                                                                     | No movement. If Claude was asked, it is marked as not financial; nothing shows in Pendientes.                                                                                                                                      |     |
| 6   | A WhatsApp message and a notification of a non-whitelisted bank.                                                                                                                                | Nothing is uploaded. With DB access: no `RawEvent` with those package names.                                                                                                                                                       |     |
| 7   | A transfer between two of your own whitelisted accounts (OUT in one, IN in the other within 30 min).                                                                                            | Pendientes → "¿Fue entre tus cuentas?" with both. "Sí" → both greyed as "Entre mis cuentas"; the header goes back to what it was before the transfer.                                                                              |     |
| 8   | Load a payment by hand, then make the same payment for real (same amount, within 10 min).                                                                                                       | Pendientes → "¿Es el mismo pago?". "Es el mismo" → the manual one is ignored, the header counts it once. "Son distintos" → both stay.                                                                                              |     |
| 9   | A notification the templates don't know (before its parser exists).                                                                                                                             | Claude reads it: the movement appears in Pendientes → "Detectamos estos gastos" (never confirmed on its own). "Está bien" confirms it; "No es un gasto" ignores it and the header rises back.                                      |     |
| 10  | An income notification (transfer received, salary).                                                                                                                                             | It appears as pending income and does **not** raise the header until you classify it (Mi sueldo / Otro ingreso / Entre mis cuentas).                                                                                               |     |
| 11  | Airplane mode, make a payment, wait 5 minutes, turn the network back on.                                                                                                                        | The card shows "N esperando" while offline; they upload by themselves after the network returns (WorkManager retry) and the queue goes back to 0.                                                                                  |     |
| 12  | Dev client only: Ajustes → Modo captura ON → receive a few notifications → "Ver muestras".                                                                                                      | Texts appear with CBU/CVU, alias, DNI, CUIT and names masked. Fix by hand what's left, "Compartir" → send them to Mauricio's chat. "Borrar todo" empties the list.                                                                 |     |
| 13  | Ajustes → Captura automática → Apagar.                                                                                                                                                          | The card goes back to "Activar". Payments after this are not captured. Android's notification access can stay on; the module has no token and uploads nothing.                                                                     |     |
| 14  | Activate again, then Cerrar sesión.                                                                                                                                                             | Payments after logout are not uploaded (the ingest token was revoked with the session).                                                                                                                                            |     |
| 15  | Eliminar mi cuenta.                                                                                                                                                                             | Every `RawEvent`, `LlmUsage` and device of the account is gone.                                                                                                                                                                    |     |
| 16  | One week of real use.                                                                                                                                                                           | Zero duplicates: `select fingerprint, count(*) from "Transaction" group by 1 having count(*) > 1` returns nothing, and every pending item in Pendientes was a real question. Cost: `select sum("costMicros")/1e6 from "LlmUsage"`. |     |
